[project]
name = "agent-observer-poc"
version = "0.1.0"
description = "Observability platform for agentic pipelines"
requires-python = ">=3.12"

dependencies = [
    "langgraph>=0.2",
    "ollama>=0.4",
]
